public class TestMutation {
    public boolean fieldVar = true;

    enum test {
        LOW,
        MEDIUM,
        HIGH
    }

    public boolean berAdditionMutation(boolean l) {
        int m = 0;
        test.HIGH;
        boolean y = true;
        boolean z = true;
        if (x == 3) {}
    }
    
    // NEED TO ADD THIS, SO THAT test() can be called and parameters dynamically resolved :p
    public boolean test() {
        return true;
    }
}